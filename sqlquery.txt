USE crud_api;

ALTER TABLE users
  ADD COLUMN password_hash VARCHAR(255) NOT NULL AFTER age,
  ADD UNIQUE KEY uq_users_email (email);

-- (Optional sample user creation removed; you'll register via API)


DROP TABLE IF EXISTS users;
CREATE TABLE users (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100),
  email VARCHAR(100) UNIQUE,
  age INT,
  password_hash VARCHAR(255) NOT NULL
);